name: flutter_grist_widgets
description: A YAML-driven Flutter application generator for Grist. Build complete data-driven apps with authentication, navigation, and forms from a simple YAML configuration.
version: 0.9.0
homepage: https://github.com/yourusername/flutter_grist_widgets

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  yaml: ^3.1.2
  provider: ^6.1.1
  crypto: ^3.0.3
  shared_preferences: ^2.2.2
  bcrypt: ^1.1.3
  file_picker: ^6.1.1
  image_picker: ^1.0.7
  mime: ^1.0.5
  intl: ^0.19.0
  shimmer: ^3.0.0
  fluttertoast: ^8.2.4
  csv: ^6.0.0
  path_provider: ^2.1.1
  excel: ^2.1.0
  pdf: ^3.10.0
  printing: ^5.11.0
  flutter_quill: ^9.3.0
  flutter_colorpicker: ^1.0.3
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8
  http: ^1.1.0

flutter:
  # This package contains widgets
