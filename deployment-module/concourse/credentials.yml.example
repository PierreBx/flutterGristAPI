# =============================================================================
# Concourse Pipeline Credentials Template
# =============================================================================
# This file contains sensitive credentials used in Concourse pipelines
#
# IMPORTANT:
# 1. Copy this file to credentials.yml
# 2. Fill in the actual values
# 3. NEVER commit credentials.yml to git (it's in .gitignore)
# 4. Use ((credential-name)) syntax in pipeline.yml to reference these values
#
# Usage in pipeline:
#   params:
#     SSH_PRIVATE_KEY: ((ssh-private-key))
#
# =============================================================================

# -----------------------------------------------------------------------------
# GIT REPOSITORY CREDENTIALS
# -----------------------------------------------------------------------------
# If using private repositories, provide SSH private key or GitHub token

# GitHub/GitLab SSH Private Key (for cloning private repos)
# To generate: ssh-keygen -t rsa -b 4096 -C "concourse-ci"
# Add public key to GitHub: Settings > SSH and GPG keys > New SSH key
github-private-key: |
  -----BEGIN OPENSSH PRIVATE KEY-----
  # Your SSH private key here (multiline)
  # Paste the entire contents of your private key
  -----END OPENSSH PRIVATE KEY-----

# OR use GitHub Personal Access Token (classic)
# Create at: GitHub Settings > Developer settings > Personal access tokens
github-access-token: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# RASPBERRY PI SSH CREDENTIALS
# -----------------------------------------------------------------------------
# SSH access to production Raspberry Pi server

# Raspberry Pi connection details
raspberry-pi-host: 192.168.1.100
raspberry-pi-user: appuser
raspberry-pi-port: 22

# SSH Private Key for deployment (should match the key in ~/.ssh/authorized_keys on Pi)
# Use a dedicated deployment key, not your personal key
# To generate: ssh-keygen -t rsa -b 4096 -f ~/.ssh/concourse_deploy_key -C "concourse-deploy"
# Then: ssh-copy-id -i ~/.ssh/concourse_deploy_key.pub appuser@192.168.1.100
ssh-private-key: |
  -----BEGIN OPENSSH PRIVATE KEY-----
  # Your deployment SSH private key here (multiline)
  # This key must be authorized on the Raspberry Pi
  -----END OPENSSH PRIVATE KEY-----

# -----------------------------------------------------------------------------
# DOCKER REGISTRY (Optional)
# -----------------------------------------------------------------------------
# If pushing Docker images to a private registry

docker-registry-url: docker.io
docker-registry-username: your-docker-username
docker-registry-password: your-docker-password

# Or use Docker Hub access token
docker-hub-token: dckr_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# NOTIFICATION CREDENTIALS (Optional)
# -----------------------------------------------------------------------------

# Slack Webhook for build notifications
# Create at: Slack App > Incoming Webhooks
slack-webhook-url: https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX

# Email/SMTP for notifications
smtp-host: smtp.gmail.com
smtp-port: 587
smtp-username: your-email@gmail.com
# For Gmail, use App Password: https://support.google.com/accounts/answer/185833
smtp-password: your-gmail-app-password
notification-email: team@example.com

# -----------------------------------------------------------------------------
# APPLICATION SECRETS (Optional)
# -----------------------------------------------------------------------------
# Secrets that your application needs at runtime

# Grist API credentials (if needed for deployment)
grist-api-key: your_grist_api_key_here
grist-server-url: http://192.168.1.100:8484

# Database credentials (if deploying database)
postgres-password: your_secure_database_password
postgres-user: app_user
postgres-database: app_db

# -----------------------------------------------------------------------------
# CLOUD PROVIDER CREDENTIALS (Optional)
# -----------------------------------------------------------------------------
# If using cloud services for backups, monitoring, etc.

# AWS credentials (for S3 backups, etc.)
aws-access-key-id: AKIAIOSFODNN7EXAMPLE
aws-secret-access-key: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
aws-region: us-east-1
s3-bucket-name: flutter-grist-backups

# -----------------------------------------------------------------------------
# SECURITY NOTES
# -----------------------------------------------------------------------------
# 1. Use strong, unique passwords for each service
# 2. Rotate credentials regularly (every 90 days recommended)
# 3. Use dedicated service accounts (not personal accounts)
# 4. Enable 2FA where possible
# 5. Monitor access logs for suspicious activity
# 6. Consider using HashiCorp Vault for production:
#    https://concourse-ci.org/creds.html#vault
#
# Vault integration example:
#   export CONCOURSE_VAULT_URL=https://vault.example.com
#   export CONCOURSE_VAULT_AUTH_BACKEND=cert
#   # Then credentials can be stored in Vault instead of this file
#
# -----------------------------------------------------------------------------
# EXAMPLE: Minimal credentials.yml for this project
# -----------------------------------------------------------------------------
#
# github-private-key: |
#   -----BEGIN OPENSSH PRIVATE KEY-----
#   ... your key ...
#   -----END OPENSSH PRIVATE KEY-----
#
# raspberry-pi-host: 192.168.1.100
# raspberry-pi-user: appuser
# ssh-private-key: |
#   -----BEGIN OPENSSH PRIVATE KEY-----
#   ... your key ...
#   -----END OPENSSH PRIVATE KEY-----
#
# -----------------------------------------------------------------------------
